/target
**/*.rs.bk
Cargo.lock
.DS_Store
*.swp
.idea/
.vscode/
omni-book/

# Local-only artifacts (Codex reviews, coverage outputs, smoke logs)
reports_fausto/

# Vault runtime data created by omninote-cli/mcp when caderno is used as a
# vault for self-testing — never commit user content or AI model caches.
.fastembed_cache/
.omninote/
Daily/
REPORTS_FAUSTO/

# Local Cargo config: shared out-of-tree target-dir with a machine-specific
# absolute path. Local-only — committing it would break CI and other machines.
/.cargo/config.toml

# Claude Code local state (agent worktrees, machine-local settings).
/.claude/

# Packaging output (scripts/make-dmg.sh) — build artifacts, never committed.
/dist/
